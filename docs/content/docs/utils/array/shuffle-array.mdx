---
title: Shuffle Array
description: Shuffles the elements of an array.
---

## shuffleArray

Shuffles the elements of an array.

### Function Signature

```typescript
function shuffleArray<T>(array: T[]): T[]
```

### Parameters

- **`array`** (`T[]`): Array to shuffle.

### Returns

- **`T[]`**: Shuffled array.

### Example

```ts pg
import { shuffleArray } from 'toolbox-x';

const numbers = [1, 2, 3, 4, 5];
const shuffledNumbers = shuffleArray(numbers);
console.log(shuffledNumbers); 
// Output: [3, 1, 5, 2, 4] (Example output; actual random result will vary)
```
