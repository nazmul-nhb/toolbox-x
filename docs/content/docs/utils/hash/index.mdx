---
title: Hash & Encoding
description: A comprehensive suite of hashing, UUID, encoding, and lightweight cryptographic utilities—including JWT-style signing, `stream-cipher` encryption, and more.
icon: Hash
---

## Overview

A comprehensive suite of hashing, UUID, encoding, and lightweight cryptographic utilities—including JWT-style signing [Signet](./hash/Signet), `stream-cipher` encryption [Cipher](./hash/Cipher), and more.

<Callout type="info" title="Info">
 Does not rely on `Node.js` or `Web APIs`. **Works on any JS engine**
</Callout>

## Imports
```ts
// All hash, UUID, encryption, and encoding utilities can be imported
// from the toolbox-x/hash module using the following pattern:
import { utilityName } from 'toolbox-x/hash';
```

## Available Utilities

### Hash Functions

- **[md5](./hash/md5)** - Compute `MD5` hash (32-character hex)
- **[sha1](./hash/sha-1)** - Compute `SHA-1` hash (40-character hex)
- **[sha256](./hash/sha-256)** - Compute `SHA-256` hash (64-character hex)

### UUID Utilities

- **[uuid](./hash/uuid)** - Generate RFC-compliant UUIDs (versions 1, 3, 4, 5, 6, 7, 8)
- **[decodeUUID](./hash/decode-uuid)** - Decode UUIDs into version, variant, and timestamp information
- **[Version Checkers](./hash/check-uuid-versions)** - Type-safe ([branded type](/docs/types/utility-types#brandedt-b)) validation for specific UUID versions

### Encryption/Authentication Tools

- **[Signet](./hash/Signet)** - Sign, decode, verify tokens in `JWT` style
- **[Cipher](./hash/Cipher)** - Encrypt, decrypt text in `stream-cipher` encryption style

### Other Utilities

- **[randomHex](./hash/random-hex)** - Generate cryptographically secure random hex strings
- **[Encoding & Crypto Utilities](./hash/encoding)** - Low-level encoding, decoding, and cryptographic functions
- **[TextCodec](./hash/TextCodec)** - Convert text between hex, binary, and Base64 formats

## Quick Start

```typescript pg
import { uuid, md5, randomHex } from 'toolbox-x/hash';

// Generate a random UUID v4
const id = uuid();

// Create MD5 hash
const hash = md5('hello world');

// Generate random hex
const hex = randomHex(16, true);

console.log({
    id,
    hash,
    hex
});
```

## Features

- **Cross-platform** - Works in Node.js, browsers, and edge runtimes
- **Type-safe** - Full TypeScript support with [branded](/docs/types/utility-types#brandedt-b) types
- **RFC compliant** - Follows UUID and hash specifications
- **No dependencies** - Pure JavaScript implementation

## Common Use Cases

- Generating unique identifiers
- Data integrity verification
- Creating random tokens and nonces
- Cryptographic hashing needs
- Encrypting/decrypting text, tokens etc.
