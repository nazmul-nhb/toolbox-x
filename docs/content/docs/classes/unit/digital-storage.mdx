---
title: Digital Storage Conversions
description: Static methods for converting between digital storage units — bytes, KB, MB, GB, and TB.
---

## mbToGb()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.mbToGb(1024)); // → 1
```

---

## gbToMb()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.gbToMb(1)); // → 1024
```

---

## kbToMb()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.kbToMb(1024)); // → 1
```

---

## kbToGb()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.kbToGb(1048576)); // → 1
```

---

## gbToKb()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.gbToKb(1)); // → 1048576
```

---

## bytesToKb()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.bytesToKb(1024)); // → 1
```

---

## kbToBytes()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.kbToBytes(1)); // → 1024
```

---

## mbToKb()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.mbToKb(1)); // → 1024
```

---

## gbToTb()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.gbToTb(1024)); // → 1
```

---

## tbToGb()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.tbToGb(1)); // → 1024
```
