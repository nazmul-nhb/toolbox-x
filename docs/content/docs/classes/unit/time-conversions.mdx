---
title: Time Conversions
description: Static methods for converting between time units — seconds, minutes, hours, and days.
---

## minutesToHours()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.minutesToHours(60)); // → 1
```

---

## secondsToMinutes()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.secondsToMinutes(60)); // → 1
```

---

## hoursToDays()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.hoursToDays(24)); // → 1
```

---

## hoursToMinutes()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.hoursToMinutes(1)); // → 60
```

---

## minutesToSeconds()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.minutesToSeconds(1)); // → 60
```

---

## daysToHours()

```ts pg
import { Unit } from 'toolbox-x';
console.log(Unit.daysToHours(1)); // → 24
```
